cmake_minimum_required(VERSION 3.10)
project(sort)
set(CMAKE_BUILD_TYPE Release)
include_directories(${CMAKE_SOURCE_DIR}/include)
file(GLOB SOURCES "../src/*.cpp")
add_library(sort SHARED ${SOURCES})
install(TARGETS sort DESTINATION ../lib)